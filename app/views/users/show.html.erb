<p id="notice"><%= notice %></p>

<p>
  <b>User:</b>
  <%= @user.email %>
</p>

<%= link_to 'Back', users_path, :class => 'btn' %> <%= link_to 'Edit', edit_user_path(@user), :class => 'btn btn-primary' if can? :update, @user %>
